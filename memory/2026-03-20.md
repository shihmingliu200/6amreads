- Built out **6amreads.com** marketing site + user flow (Next 14 + Tailwind): landing, signup/login with Google option, 7-screen onboarding, dashboard (profile, newspaper preview, settings, feedback, pause).
- Extended API: migration `002_user_prefs_engagement.sql`, `POST /auth/google`, `PATCH /profile`, admin routes mounted on Express (`/admin/*`), expanded stats/status, member PATCH/DELETE, cron status + default 6 AM schedule; daily job skips `paused` users.
